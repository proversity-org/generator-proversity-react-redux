{
  "name": "<%= name %>",
  "version": "<%= version %>",
  "description": "<%= description %>",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && webpack --env=prod -p --progress",
    "start": "webpack-dev-server --host 0.0.0.0 --port 3000 --env=dev --progress --inline",
    "test": "karma start"
  },
  "repository": {
    "type": "git",
    "url": "<%= repo %>"
  },
  "keywords": [],
  "author": "<%= author %>",
  "dependencies": {},
  "devDependencies": {}
}